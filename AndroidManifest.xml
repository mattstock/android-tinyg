<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.csgeeks.TinyG"
    android:versionCode="1"
    android:versionName="@string/version" >

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="8"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/tinyg"
        android:label="@string/app_name" >
        <activity
            android:name=".TinyGActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
              	<action android:name="org.csgeeks.TinyG.UPDATE" />
            </intent-filter>
        </activity>
        <activity android:name=".ShowSettingsActivity"></activity>
        <activity android:name=".MachineActivity"></activity>
        <service android:name=".TinyGDriver"></service>
    </application>

</manifest>